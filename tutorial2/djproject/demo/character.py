# from djproject.demo import models
# from .models import Items
#
#
# class Character(models.Model):
#     name = models.CharField(max_length=30)
#     items = models.ForeignKey(Items, on_delete=models.CASCADE, related_name='characters')